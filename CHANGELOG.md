# 更新日志 Changelog

本项目所有值得留意的版本变更都记录在此。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

发布 `vX.Y.Z` tag 时，CI 会自动把对应版本段落作为 GitHub Release 的说明。早于 0.1.9 的版本变更未整理到本文件，可查看 [GitHub Releases](https://github.com/iAmbitions/CXX/releases) 与提交历史。

## [0.1.19] - 2026-08-27

### 修复
- **手机与电脑同名会话内容不一致、桌面运行中的会话未进入“进行中”**。Codex Desktop 在首轮被中断后继续同一任务时，可能为同一个 threadId 新建带轮次后缀的 rollout 文件，而 app-server 仍返回最初的旧文件路径。口袋 Agent 现在会跨日期检查同 threadId 的候选文件并选择实际最新写入的 rollout，使手机历史、实时增量和桌面活动状态都跟随电脑当前会话。

## [0.1.18] - 2026-08-27

### 性能
- **加速 OpenCode 会话与模型加载**。显式配置模型时直接复用 OpenCode 的轻量配置响应，不再下载约数 MB 的完整公共模型目录；模型请求增加启动预热、60 秒缓存与并发合并，避免多设备或网络切换时重复读取。
- **避免重连风暴**。手机端仅在 daemon 进程实例真正变化时清空模型缓存，普通中继重连、切换前后台以及 WebRTC 升级/回落不再反复拉取模型列表。

## [0.1.17] - 2026-08-27

### 修复
- **OpenCode 模型列表过多**。OpenCode 的 provider API 会返回所有已连接服务的完整公共模型目录；现在检测到用户显式配置的模型后，只展示这些配置项，并保留默认模型、小模型与 Agent 专用模型引用，避免手机端出现上百个无关模型。未配置模型白名单的用户仍可看到原有完整列表。

## [0.1.16] - 2026-08-27

### 新增
- **OpenCode 一等支持**。手机端可像 ChatGPT 与 Claude Code 一样浏览和继续 OpenCode 会话，支持实时流式内容、工具调用、审批、问答、模型与推理档位、Fork、归档和中断；同时补齐常见安装路径、版本管理器、环境变量与 provider token 的发现逻辑。

### 修复
- **手机退出后桌面仍提示会话被占用**。最后一个手机观察者离开且轮次结束后，主动释放 ChatGPT app-server 订阅；多设备与运行中任务按需延迟释放，避免桌面端长期无法继续会话。

### 调整
- **移除“检查更新”**。macOS 菜单栏、Windows 托盘和命令行不再提供联网检查更新入口。
- **统一产品名称为 Pocket Agent / 口袋 Agent**。README、发布标题、中继说明、服务描述和用户可见提示不再使用旧品牌名称；底层命令、配置目录和环境变量继续保留兼容。

## [0.1.12] - 2026-08-02

### 新增
- **OneBot 11（NapCat）QQ 通知渠道**。支持通过 NapCat HTTP 服务器向 QQ 私聊或群聊发送口袋 Agent 任务通知，可选 Bearer 令牌，并校验 HTTP 与 OneBot 业务响应。CLI 增加目标与地址校验，同步补充中英文说明及回归测试。

## [0.1.11] - 2026-07-17

### 修复
- **Windows 中文账户名无法开启口袋 Agent**。不再把 `whoami` 的本地代码页输出误当 UTF-8；改用 Windows 身份 API 获取规范账户名并显式输出 UTF-8，环境变量回退路径也保持 Unicode，确保中文电脑名、域名和用户名能完整写入计划任务的 `UserId`。

## [0.1.10] - 2026-07-14

### 修复
- **手机浏览 Claude 会话时，带图片的用户消息丢失文字**。此前 daemon 只识别 codex 的图片格式，Claude 贴图的内联 base64 未被抽出缓存，整条消息超过截断上限后被替换为空壳、网页拒绝渲染，只剩一条内部图片路径气泡。现 daemon 支持抽取 Claude 图片块（inline base64 → 缓存引用），文字保留、图片经分块推流正常显示；网页同时过滤 `isMeta` 簿记条目（贴图源路径、尺寸备注、caveat、continue 提示不再冒充用户气泡）。对历史会话同样生效。
- **会话标题过长时撑破标题栏**。标题中间列改为可收缩的网格列，超长自动截断为省略号，不再挤压两侧的返回 / 分享 / 更多按钮。

### 精简
- 移除会话「…」菜单里的「在终端中打开此项目」——价值有限，且与手动新建终端高度重合。

## [0.1.9] - 2026-07-14

### 新增
- **Terminal Mode —— 手机远程接管本机终端**。agent 选择器新增「终端」入口：手机上新建终端窗口、直连软键盘输入、xterm 完整渲染回显。App 内嵌 `pty-host` 二进制，SEA 打包与 GitHub Pages 网页端均可用。
- **可编辑的启动命令**。新建窗口的「启动命令」由固定探测改为「默认 + 用户可编辑」：内置 Claude Code、Shell 两项，手机端即可修改默认项、增删自定义命令（存入 `daemon.json`，热加载生效）。
- **真·终端交互**。去掉底部输入栏，直接用软键盘打字；快捷键收进一个悬浮抽屉，悬浮钮可长按拖动并记忆位置，抽屉展开方向随位置自适应。
- **触摸滚动**。手指上下拖动驱动 xterm 回滚区；grok / vim 等全屏 TUI 自动改发 SGR 滚轮事件翻页，不再误触发方向键。
- 新建窗口的工作目录支持实时筛选（最近使用 + 关键词），项目多时也好找。
- 终端列表区分「运行中 / 空闲」，长按即可结束窗口（统一样式确认框，破坏性操作红色变体）。

### 修复
- 设备授权 / 撤销对以 `-` 开头的 `deviceId` 失效的问题。
- 命令型启动命令改走交互式登录 shell（`-l -i -c`），修复 alias / shell 函数「command not found」。
- 计划清单标记在 iOS / 微信里被渲染成彩色 emoji 方块（改用文本呈现符 + 圆点）。
- 后台任务通知行对齐桌面端样式（左对齐 ● 事件文本）。

### 术语
- 终端「实例」统一改称「窗口」，描述文案用「终端窗口」；「启动方式」改称「启动命令」。
